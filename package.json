{
  "name": "tiketera-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "build:api": "pnpm --filter api build"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
